target-version = "py39"
line-length = 88

extend-exclude = [
    "build",
    "docs",
    "*.egg-info",
]

[lint]
# Keep the initial rule set small: correctness, essential style checks and imports.
select = ["E4", "E7", "E9", "F", "I"]
